name: CI Test

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v2

      - name: Afficher un message
        run: echo "🎉 Ton pipeline GitHub fonctionne !"
